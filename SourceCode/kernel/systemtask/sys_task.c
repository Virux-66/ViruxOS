#include "type.h"
#include "prototype.h"
#include "const.h"

PUBLIC void sys_task() {
	MESSAGE meg;


	while (1) {
		/*send_recv(RECEIVE, ANY, &meg);
		int src = meg.source;
		printf("%x", &meg.source);

		delayInMilli(200);*/
	}
}